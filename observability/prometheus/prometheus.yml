global:
  scrape_interval: 15s

scrape_configs:
  - job_name: todolist-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["backend:8080"]
    authorization:
      credentials: local-observability-demo-token

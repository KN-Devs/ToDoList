{
  "name": "e2e",
  "version": "1.0.0",
  "description": "Tests end-to-end (Playwright) du ToDoList, contre le frontend et le backend réels",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1"
  }
}
